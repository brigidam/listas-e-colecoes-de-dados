package calculos;

// Método abstrato que deve ser implementado por qualquer classe que utilize essa interface.
// Esse método retorna um inteiro que representa a classificação de um determinado objeto.
public interface Classificavel {
    int getClassificacao();
}
